export interface OrdreMissionRequest {
  dateDebut: string;  // 'dd/MM/yyyy'
  heureDebut: string; // 'HH:mm'
  dateFin: string;    // 'dd/MM/yyyy'
  heureFin: string;   // 'HH:mm'
  missionObjet: string;
}
